export enum Mode {
  New = "New",
  Edit = "Edit",
  View = "View",
}

export enum RequestStatus {
  New = "New"
}

export enum Action {
  Add = "Add",
  Edit = "Edit",
  Update = "Update",
  Cancel = "Cancel",
  Delete = "Delete",
}
